---
title: Tabs
description: A tabs component with accessible tab navigation.
---

<ComponentPreview
  name="tabs-demo"
  description="Tabs with accessible keyboard navigation."
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx @preskok-org/ui@latest add tabs
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="tabs" title="registry/preskok/ui/preskok-ui/tabs.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Tabs } from "@/registry/preskok/ui/preskok-ui/tabs"

export function TabsDemo() {
  return (
    <Tabs defaultSelectedKey="1" items={[{ id: "1", title: "Tab 1" }]}>
      {(item) => <div>{item.title}</div>}
    </Tabs>
  )
}
```
