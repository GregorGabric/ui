{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "form",
  "type": "registry:ui",
  "description": "A form wrapper component with accessibility features",
  "dependencies": [
    "react-aria-components"
  ],
  "files": [
    {
      "path": "registry/new-york-v4/ui/preskok-ui/form.tsx",
      "content": "\"use client\"\n\nimport type { FormProps as FormPrimitiveProps } from \"react-aria-components\"\nimport { Form as FormPrimitive } from \"react-aria-components\"\n\ninterface FormProps extends FormPrimitiveProps {\n  ref?: React.RefObject<HTMLFormElement>\n}\n\nconst Form = ({ ref, ...props }: FormProps) => {\n  return <FormPrimitive ref={ref} {...props} />\n}\n\nexport type { FormProps }\nexport { Form }\n",
      "type": "registry:ui"
    }
  ]
}